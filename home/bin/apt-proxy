#!/bin/bash

if [[ -z "$1" ]]; then
  echo "Disabling apt proxy"
  sudo rm /etc/apt/apt.conf.d/000proxy
else
  echo "Setting apt proxy to $1"
  echo "Acquire::http { proxy \"http://$1:3142\" }" | sudo tee /etc/apt/apt.conf.d/000proxy
fi

